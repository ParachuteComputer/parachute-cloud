{
  "name": "@openparachute/vault-do",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vault Cloud — per-tenant Vault Durable Object + edge router serving the vault wire contract on DO SQLite (@openparachute/core behind a Database-shaped shim), R2 attachments, scope-guard auth against the Identity Worker.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openparachute/core": "file:../../../parachute-vault/core",
    "@openparachute/scope-guard": "0.5.0",
    "jose": "^6.2.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.57",
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.6.0",
    "vitest": "~3.2.0",
    "wrangler": "^4.26.0"
  }
}

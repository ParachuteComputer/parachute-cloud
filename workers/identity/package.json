{
  "name": "@openparachute/identity-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloud Identity Worker (Phase 3): a Cloudflare Worker + D1 reproducing the hub's OAuth issuer contract exactly, so Claude.ai / Claude Code / ChatGPT connectors + surface-client work against cloud vaults unchanged.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "seed:dev": "bun scripts/seed-dev-user.ts && wrangler d1 execute parachute-identity --local --file=./scripts/seed-dev-user.sql"
  },
  "dependencies": {
    "@paulmillr/qr": "^0.3.0",
    "hono": "^4.6.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.57",
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.6.0",
    "vitest": "~3.2.0",
    "wrangler": "^4.26.0"
  }
}

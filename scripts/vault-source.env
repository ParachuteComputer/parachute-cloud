# Exact parachute-vault source used by Cloud's shared @openparachute/core
# file dependency. CI and local deploys verify this commit so unrelated
# upstream work or a developer branch cannot change a build or deployment
# (cloud#45, cloud#59).
# Pinned at parachute-vault main's tip as of 2026-08-10: release 0.7.6-rc.2
# (#651, long_lived opt-in raises manage-token mint TTL cap to 90 days).
# core/package.json is still 0.1.0 — unaffected by that change.
# Bump both values deliberately when promoting vault-core.
VAULT_CORE_VERSION="0.1.0"
VAULT_CORE_REF="921f1df52967af580deeabe8fabd98b9c5349f71"

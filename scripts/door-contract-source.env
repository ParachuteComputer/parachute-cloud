# Exact Hub source used to build Cloud's shared @openparachute/door-contract
# file dependency. door-contract 0.6.x IS published to npm now, but Cloud
# vendors from a pinned Hub git commit for exact-source-match (the reproducible
# link between this Cloud release and the exact contract source it embeds).
# 0.6.1 (Hub #770) adds the composed account-scope grammar: parseComposedAccountScope,
# composedAccountGrant, the composed builders, and fail-closed slot validation.
DOOR_CONTRACT_VERSION="0.6.1"
DOOR_CONTRACT_HUB_REF="39a998f0e6e7bb7584e7e495b290258ba54856a9"
